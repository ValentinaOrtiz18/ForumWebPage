version: '3.8'

services:
  forum:
    build: .
    container_name: forum-app
    ports:
      - "3030:3030"
    volumes:
      - forum-data:/app
    restart: unless-stopped
    environment:
      - CGO_ENABLED=1

volumes:
  forum-data:
